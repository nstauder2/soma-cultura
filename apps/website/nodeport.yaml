apiVersion: v1
kind: Service
metadata:
  name: static-website
  namespace: soma
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: static-website
  type: NodePort
